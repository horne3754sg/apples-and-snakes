
###Apples and Snakes

####Setup & Info

To being run `npm install` to install the node_modules

To run the SCSS compiler, in Terminal run `gulp watch` - only required if you are changing any SCSS files which are used for the front end.

After updating SCSS, you can bump the Version Constant `AAS_VERSION` in functions.php

